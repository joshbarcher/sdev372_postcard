# The build context is the WHOLE folder -- everything here is sent to the
# daemon before the first instruction runs. node_modules is the big one: it is
# copied in, then thrown away by `npm install` on a different platform anyway.
node_modules
npm-debug.log
.env
.git

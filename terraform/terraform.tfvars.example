# Copy to terraform.tfvars, which is gitignored.
project_id  = "your-gcp-project-id"
admin_token = "pick-something"

# Narrow this to your own address if you would rather not publish port 8080.
# allowed_cidrs = ["203.0.113.4/32"]

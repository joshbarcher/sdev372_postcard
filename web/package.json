{
	"name": "postcard-web",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Postcard front end — the page SDEV 372 puts in front of the API",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"test": "vitest run",
		"lint": "eslint ."
	},
	"dependencies": {
		"react": "^19.1.0",
		"react-dom": "^19.1.0"
	},
	"devDependencies": {
		"@vitejs/plugin-react": "^4.3.4",
		"eslint": "^9.37.0",
		"vite": "^6.0.7",
		"vitest": "^3.2.4"
	}
}
